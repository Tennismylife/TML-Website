'use client';
import React from 'react';

// Minimal client component placeholder for compatibility.
export default function TennisMatchDatabaseClient() {
  // This component intentionally does not emit JSON-LD or <head> tags.
  // The authoritative Dataset JSON-LD is injected server-side in
  // `app/tennis-match-database/head.tsx` to avoid duplicates.


  return <div style={{ display: 'none' }} />;
}
