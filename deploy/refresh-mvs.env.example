# Example environment file for refresh-mvs PM2 process
# Copy this to a secure location on the server (e.g. /etc/default/refresh-mvs.env) and edit the DATABASE_URL

DATABASE_URL="postgresql://postgres:REPLACE_ME@localhost:5432/tennis"
MV_REFRESH_DEBOUNCE_MS=5000
REFRESH_CONCURRENTLY=0
