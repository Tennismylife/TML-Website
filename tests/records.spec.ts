// Playwright E2E test migrated to `tests/records.playwright.ts` to avoid Vitest parsing the Playwright `test()` global.
// Run Playwright tests with `npx playwright test` or `npm run test:e2e` if configured.

import { it } from 'vitest';

// Keep a skipped placeholder so Vitest doesn't treat this as a file with zero tests
it.skip('playwright placeholder', () => {});

